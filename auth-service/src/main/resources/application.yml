server:
  port: 8080
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/nexushr_db
    username: postgres
    password: admin123 # Matches what you set earlier!
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true